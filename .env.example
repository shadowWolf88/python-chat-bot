# Healing Space Environment Configuration
# SECURITY: This is an example file. Never commit real .env to git!
# Copy to .env and fill in actual values for your deployment

# ========== RUNTIME CONFIGURATION ==========
# DEBUG=1 for development, DEBUG=0 for production
DEBUG=0

# ========== CRITICAL: DATABASE CREDENTIALS (REQUIRED) ==========
# Option A: Railway deployment (preferred)
DATABASE_URL=postgresql://user:password@host:port/database

# Option B: Manual PostgreSQL connection (if not using DATABASE_URL)
# All of these are REQUIRED if DATABASE_URL is not set
DB_HOST=localhost
DB_PORT=5432
DB_NAME=healing_space
DB_USER=postgres
DB_PASSWORD=NEVER_HARDCODED_USE_ENV_VARS

# ========== CRITICAL: SESSION ENCRYPTION KEY (REQUIRED) ==========
# PRODUCTION: Must be set explicitly
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
# This must be 64+ hex characters (32+ bytes)
SECRET_KEY=your_64_char_hex_string_here_REQUIRED_IN_PRODUCTION

# ========== SECURITY: PIN SALT (REQUIRED) ==========
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
PIN_SALT=your_random_pin_salt_here

# ========== SECURITY: ANONYMIZATION SALT (TIER 1.10 - REQUIRED) ==========
# Used to anonymize usernames in training data before AI training
# CRITICAL: This salt is permanent. Changing it invalidates all anonymized records.
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
# Must be 32+ characters (64+ hex = 32+ bytes random)
# Production: Must be explicitly set, no auto-generation
ANONYMIZATION_SALT=your_64_char_hex_string_for_anonymization
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Must be 44 characters (base64-encoded 256-bit key)
ENCRYPTION_KEY=your_44_char_fernet_key_here_base64

# ========== AI SERVICE CONFIGURATION ==========
# Groq API key (required for AI features)
# Get from: https://console.groq.com
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
API_URL=https://api.groq.com/openai/v1/chat/completions

# ========== EMAIL CONFIGURATION (OPTIONAL) ==========
SMTP_SERVER=smtp.zoho.eu
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your_email_password_here
FROM_EMAIL=noreply@healing-space.org.uk
APP_URL=https://healing-space.org.uk

# ========== SECURITY: DEVELOPER REGISTRATION (OPTIONAL) ==========
# Required to register the single developer account
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
DEVELOPER_REGISTRATION_KEY=your_secret_developer_key_here

# ========== VAULT INTEGRATION (OPTIONAL) ==========
# HashiCorp Vault for secrets management
VAULT_ADDR=https://vault.example.com
VAULT_TOKEN=your_vault_token_here

# ========== SFTP EXPORT (OPTIONAL) ==========
# For exporting training data via SFTP
SFTP_HOST=sftp.example.com
SFTP_PORT=22
SFTP_USER=username
SFTP_PASS=password
SFTP_PKEY_PATH=/path/to/private/key
SFTP_REMOTE_DIR=/remote/directory

# ========== WEBHOOK ALERTS (OPTIONAL) ==========
# For critical alerts (e.g., Slack)
ALERT_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ========== CORS CONFIGURATION (OPTIONAL) ==========
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://healing-space.org.uk,https://www.healing-space.org.uk

# ========== FEATURE FLAGS (OPTIONAL) ==========
DISABLE_CSRF=0
ENABLE_GDPR_EXPORT=1
