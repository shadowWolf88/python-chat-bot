# Additional dependencies for AI training system
# These are optional - app works without them using Groq
# Install with: pip install -r requirements-training.txt

# Core ML libraries
transformers>=4.35.0
torch>=2.0.0
datasets>=2.14.0
accelerate>=0.24.0

# Training utilities
numpy>=1.24.0
scikit-learn>=1.3.0

# Optional: For GPU acceleration
# Install separately if you have CUDA:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
